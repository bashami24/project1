import React, { useState, useEffect } from 'react';
import { getAllProductsAPI, addProductAPI, updateProductAPI, deleteProductAPI } from './ProductService';

const ProductPage = () => {
    const [products, setProducts] = useState([]);
    const [newProductName, setNewProductName] = useState('');
    const [newProductPrice, setNewProductPrice] = useState(0);
    const [newProductSellerId, setNewProductSellerId] = useState(0);

    useEffect(() => {
        getAllProductsAPI().then(data => setProducts(data));
    }, []);

    const handleAddProduct = () => {
        addProductAPI(newProductName, newProductPrice, newProductSellerId).then(() => {
            getAllProductsAPI().then(data => setProducts(data));
            setNewProductName('');
            setNewProductPrice(0);
            setNewProductSellerId(0);
        });
    };

    const handleUpdateProduct = (productId, newName, newPrice, newSellerId) => {
        updateProductAPI(productId, newName, newPrice, newSellerId).then(() => {
            getAllProductsAPI().then(data => setProducts(data));
        });
    };

    const handleDeleteProduct = (productId) => {
        deleteProductAPI(productId).then(() => {
            getAllProductsAPI().then(data => setProducts(data));
        });
    };

    return (
        <div>
            <h1>Products</h1>
            <ul>
                {products.map(product => (
                    <li key={product.productId}>
                        {product.name} - {product.price} - {product.sellerId}
                        <button onClick={() => handleUpdateProduct(product.productId, product.name, product.price, product.sellerId)}>Update</button>
                        <button onClick={() => handleDeleteProduct(product.productId)}>Delete</button>
                    </li>
                ))}
            </ul>
            <h2>Add New Product</h2>
            <input type="text" placeholder="Name" value={newProductName} onChange={(e) => setNewProductName(e.target.value)} />
            <input type="number" placeholder="Price" value={newProductPrice} onChange={(e) => setNewProductPrice(parseFloat(e.target.value))} />
            <input type="number" placeholder="Seller ID" value={newProductSellerId} onChange={(e) => setNewProductSellerId(parseInt(e.target.value))} />
            <button onClick={handleAddProduct}>Add Product</button>
        </div>
    );
};

export default ProductPage;
