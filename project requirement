const myFunction = () => {
  const thisFunction = () => {
    // something
  };
  return <button onClick={thisFunction}>Something!</button>;
};
`There are ${product.quantity} units of ${product.name} at $${product.price}`

const theseThings= [1,2,3,4,5]
const allOfThem = theseThings.map((thisThing) =>
  <li>{allOfThem}</li>
);

return (
  <>
    {error && (
      <h1>Error</h1>
    )}
  </>
)
